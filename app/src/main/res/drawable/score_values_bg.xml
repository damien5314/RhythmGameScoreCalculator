<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <stroke
        android:width="@dimen/score_values_background_width"
        android:color="@color/score_values_background"/>

    <padding
        android:top="@dimen/score_values_background_padding"
        android:right="@dimen/score_values_background_padding"
        android:bottom="@dimen/score_values_background_padding"
        android:left="@dimen/score_values_background_padding"/>

    <corners
        android:topLeftRadius="@dimen/score_values_bg_corners"
        android:topRightRadius="@dimen/score_values_bg_corners"
        android:bottomLeftRadius="@dimen/score_values_bg_corners"
        android:bottomRightRadius="@dimen/score_values_bg_corners"/>

    <solid
        android:color="@android:color/transparent"/>

</shape>